{
  "name": "purescript-node-datagram",
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "jshint src && jscs src && pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "author": "Brandon Hamilton",
  "license": "MIT",
  "devDependencies": {
    "jscs": "^3.0.7",
    "jshint": "^2.9.4",
    "pulp": "^10.0.4",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1"
  }
}
